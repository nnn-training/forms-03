// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model User {
  userId       String   @id @default(auto()) @map("_id") @db.ObjectId
  authjsUserId String   @unique
  username     String
  isAdmin      Boolean  @default(false)
  forms        Form[]
  answers      Answer[]

  @@map("users")
}

model Form {
  formId      String     @id @default(auto()) @map("_id") @db.ObjectId
  formTitle   String
  description String
  createdBy   String     @db.ObjectId
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
  user        User       @relation(fields: [createdBy], references: [userId], onDelete: Cascade)
  questions   Question[]
  answers     Answer[]

  @@index([createdBy])
  @@map("forms")
}

model Question {
  questionId   String   @id @default(auto()) @map("_id") @db.ObjectId
  questionText String
  questionType String
  choices      String[]
  formId       String   @db.ObjectId
  form         Form     @relation(fields: [formId], references: [formId], onDelete: Cascade)
  answers      Answer[]

  @@index([formId])
  @@map("questions")
}

model Answer {
  answerId   String   @id @default(auto()) @map("_id") @db.ObjectId
  answer     String
  answeredBy String   @db.ObjectId
  formId     String   @db.ObjectId
  questionId String   @db.ObjectId
  user       User     @relation(fields: [answeredBy], references: [userId])
  form       Form     @relation(fields: [formId], references: [formId], onDelete: Cascade)
  question   Question @relation(fields: [questionId], references: [questionId], onDelete: Cascade)

  @@unique([answeredBy, questionId])
  @@index([formId])
  @@map("answers")
}
